#include <stdio.h>

/**
 * The main function of this program. Simply confirms that the file has built successfully
 * @return Default return for main function
 */
int main() {
    printf("Successfully built and run");
    return 0;
}